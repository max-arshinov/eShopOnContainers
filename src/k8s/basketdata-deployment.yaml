apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml -f docker-compose.override.yml -o k8s --volumes hostPath convert
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: basketdata
  name: basketdata
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: basketdata
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose -f docker-compose.yml -f docker-compose.override.yml -o k8s --volumes hostPath convert
        kompose.version: 1.26.1 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: basketdata
    spec:
      containers:
        - image: redis:alpine
          name: basketdata
          ports:
            - containerPort: 6379
          resources: {}
          volumeMounts:
            - mountPath: /data
              name: eshop-basketdata
      restartPolicy: Always
      volumes:
        - hostPath:
            path: <YOUR PATH>/eShopOnContainers/src/volumes/basket-data
          name: eshop-basketdata
status: {}
