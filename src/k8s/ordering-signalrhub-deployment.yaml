apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml -f docker-compose.override.yml -o k8s --volumes hostPath convert
    kompose.service.type: LoadBalancer
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: ordering-signalrhub
  name: ordering-signalrhub
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: ordering-signalrhub
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose -f docker-compose.yml -f docker-compose.override.yml -o k8s --volumes hostPath convert
        kompose.service.type: LoadBalancer
        kompose.version: 1.26.1 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: ordering-signalrhub
    spec:
      containers:
        - env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ASPNETCORE_URLS
              value: http://0.0.0.0:80
            - name: ApplicationInsights__InstrumentationKey
            - name: AzureServiceBusEnabled
              value: "False"
            - name: EventBusConnection
              value: docker.for.mac.localhost
            - name: EventBusPassword
            - name: EventBusUserName
            - name: OrchestratorType
            - name: identityUrl
              value: http://identity-api
          image: eshop/ordering.signalrhub:linux-latest
          name: ordering-signalrhub
          ports:
            - containerPort: 80
          resources: {}
      restartPolicy: Always
status: {}
